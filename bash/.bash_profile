# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
  . ~/.bashrc
fi
if [ -e /home/niteria/.nix-profile/etc/profile.d/nix.sh ]; then . /home/niteria/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer
